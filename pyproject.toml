[project]
name = "pseti-gui"
version = "0.5.1"
requires-python = ">=3.14"
dependencies = [
    "pyqt6",
    "pyqtgraph",
    "numpy",
    "panoseti-grpc>=0.4.17",
    "typer",
    "python-dotenv",
    "rich",
    "psutil",
]

[project.scripts]
pseti-gui = "pseti_gui.app:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/pseti_gui"]
